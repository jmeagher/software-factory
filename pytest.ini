[pytest]
testpaths = tests
markers =
    llm_eval: mark test as an LLM invocation eval (skipped unless --run-llm-evals is passed)
    integration: mark test as an integration test requiring a live OTel stack (skips when env vars absent)
